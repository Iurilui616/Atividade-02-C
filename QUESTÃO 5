#include <stdio.h>

int main() {
    int altura, pesoIdeal;

    printf("Digite a altura do homem (em cm): ");
    scanf("%d", &altura);

    pesoIdeal = (72.7 * altura) - 58;

    printf("O peso ideal e: %d kg\n", pesoIdeal);

    return 0;
}
